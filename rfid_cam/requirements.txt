# Serial communication
pyserial>=3.5

# Configuration management
python-dotenv>=1.0.0

# GPS support
pynmea2>=1.19.0

# Image metadata and EXIF handling
Pillow>=10.0.0
piexif>=1.1.3

# AI Integration
openai>=1.0.0

# HTTP requests
requests>=2.25.0

# Email support (built-in to Python standard library)
# smtplib and ssl are included

# Note: Raspberry Pi specific packages (install via apt):
# sudo apt install python3-picamera2 libcamera-apps rclone

# SMS via Google Fi email gateway (no Twilio needed)